# Local development only. Zeabur single-service deploy does not need this file.

# Frontend dev server port
PORT=3000

# Backend API base URL used by Vite dev proxy during local development.
BACKEND_BASE_URL=http://127.0.0.1:8000
